[package]
name = "lobster-lua"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde = { version = "1.0.228", features = ["derive"] }
expect-test = "*"
